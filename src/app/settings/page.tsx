'use client';import {useRef,useState} from 'react';import {Download,Upload,Trash2,RotateCcw} from 'lucide-react';import {useVocabularyStore} from '@/store/useVocabularyStore';import {backup} from '@/lib/storage';
export default function Settings(){const s=useVocabularyStore(),file=useRef<HTMLInputElement>(null),[msg,setMsg]=useState('');const load=async(e:React.ChangeEvent<HTMLInputElement>)=>{try{const f=e.target.files?.[0];if(!f)return;const d=JSON.parse(await f.text());if(!Array.isArray(d.words)||!Array.isArray(d.categories)||!Array.isArray(d.sessions))throw 0;s.replaceData(d);setMsg(`Импортировано ${d.words.length} слов`)}catch{setMsg('Некорректный файл резервной копии')}};return <div className="max-w-2xl"><p className="muted">Персонализация и данные</p><h1 className="text-3xl font-black mb-6">Настройки</h1><section className="card p-5 space-y-5 mb-5"><h2 className="font-bold text-lg">Внешний вид</h2><label className="block"><span className="text-sm font-bold">Тема</span><select className="mt-2" value={s.settings.theme} onChange={e=>s.setSettings({theme:e.target.value as 'light'|'dark'|'system'})}><option value="system">Как в системе</option><option value="light">Светлая</option><option value="dark">Тёмная</option></select></label></section><section className="card p-5 space-y-5 mb-5"><h2 className="font-bold text-lg">Обучение</h2><label className="block">Карточек за сеанс: {s.settings.cardsPerSession}<input type="range" min="5" max="50" value={s.settings.cardsPerSession} onChange={e=>s.setSettings({cardsPerSession:+e.target.value})}/></label>{[['showExamples','Показывать примеры'],['shuffle','Перемешивать слова'],['reverse','Обратные вопросы'],['autoCategory','Автокатегории']].map(([k,l])=><label className="flex justify-between" key={k}>{l}<input className="w-5" type="checkbox" checked={Boolean(s.settings[k as keyof typeof s.settings])} onChange={e=>s.setSettings({[k]:e.target.checked})}/></label>)}</section><section className="card p-5"><h2 className="font-bold text-lg mb-4">Мои данные</h2><div className="grid sm:grid-cols-2 gap-3"><button className="btn secondary" onClick={()=>backup({words:s.words,categories:s.categories,sessions:s.sessions,settings:s.settings})}><Download size={18}/>Экспорт JSON</button><button className="btn secondary" onClick={()=>file.current?.click()}><Upload size={18}/>Импорт JSON</button><input ref={file} hidden type="file" accept="application/json" onChange={load}/><button className="btn border" onClick={s.resetProgress}><RotateCcw size={18}/>Сбросить прогресс</button><button className="btn danger-action sm:col-span-2" onClick={s.clear}><Trash2 size={18}/>Удалить все данные</button></div>{msg&&<p className="muted mt-4">{msg}</p>}</section></div>}
