'use client';import {useMemo,useState} from 'react';import Link from 'next/link';import {useVocabularyStore} from '@/store/useVocabularyStore';import {Progress} from '@/components/ui/Parts';
export default function Quiz(){const s=useVocabularyStore(),[i,setI]=useState(0),[score,setScore]=useState(0),[picked,setPicked]=useState('');const words=useMemo(()=>s.words.filter(w=>w.language===s.selectedStudyLanguage),[s.words,s.selectedStudyLanguage]),qs=useMemo(()=>words.slice(0,10),[words]),options=useMemo(()=>{const current=qs[i];if(!current)return [];return [current,...words.filter(w=>w.id!==current.id).sort(()=>.5-Math.random()).slice(0,3)].sort(()=>.5-Math.random())},[i,qs,words]);if(words.length<4)return <div className="card p-10 text-center"><h1 className="text-2xl font-bold">Нужно минимум 4 слова</h1><p className="muted my-4">Добавьте ещё слов выбранного языка, чтобы варианты ответов были корректными.</p><Link className="btn primary" href="/add">Добавить слова</Link></div>;if(i>=qs.length)return <div className="card p-8 text-center"><h1 className="text-3xl font-black">Результат: {score} из {qs.length}</h1><p className="muted my-5">Точность {Math.round(score/qs.length*100)}%</p><Link href="/" className="btn primary">На главную</Link></div>;const q=qs[i];return <div className="max-w-xl mx-auto"><p className="muted text-sm mb-2">Вопрос {i+1} из {qs.length}</p><Progress value={i/qs.length*100}/><div className="card p-7 mt-6 text-center"><span className="pill">{q.language==='english'?'EN':'DE'}</span><h1 className="text-4xl font-black my-8">{q.word}</h1><div className="grid gap-3">{options.map(o=><button key={o.id} disabled={!!picked} onClick={()=>{setPicked(o.id);if(o.id===q.id)setScore(x=>x+1)}} className={`btn border ${picked?(o.id===q.id?'answer-correct':picked===o.id?'answer-wrong':''):''}`}>{o.translationRu}</button>)}</div>{picked&&<button className="btn primary w-full mt-5" onClick={()=>{setI(x=>x+1);setPicked('')}}>Продолжить</button>}</div></div>}
